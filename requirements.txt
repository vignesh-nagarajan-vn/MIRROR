# MIRROR — core ML stack (root). Backend-only deps live in backend/requirements.txt.
# Install: pip install -r requirements.txt

# Deep learning
torch>=2.1
torchvision>=0.16

# Imaging
Pillow>=10.0
numpy>=1.24
matplotlib>=3.7        # colormaps for saliency overlays
pydicom>=2.4           # native DICOM (.dcm) ingestion: modality/VOI LUT, windowing
# Optional: decode compressed DICOM (JPEG/JPEG2000) transfer syntaxes.
# Uncompressed DICOM works without these.
pylibjpeg>=2.0; python_version >= "3.8"
pylibjpeg-libjpeg>=2.1; python_version >= "3.8"

# Data / metrics
pandas>=2.0
scikit-learn>=1.3      # AUROC, F1

# Config
PyYAML>=6.0

# Report generation (optional backend; template backend works without a key)
anthropic>=0.39

# Notebooks (optional)
jupyter>=1.0

# Small real benchmark for CPU-scale train/eval (optional; datasets/scripts/prepare_chestmnist.py)
medmnist>=3.0

# Tests (dev)
pytest>=7.0
